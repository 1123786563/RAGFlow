### 布局优化计划

1. **修复容器高度计算**
   - 将 `parentRef` 恢复到 `CardContent` 元素上，确保容器高度计算正确
   - 调整 `containerHeight` 计算公式，确保虚拟滚动区域正确填充可用空间

2. **优化表格宽度和列布局**
   - 为 `Table` 组件添加 `w-full` 类，确保表格占满可用宽度
   - 调整 `colgroup` 中的列宽定义，使用更灵活的宽度分配：
     - 选择列：固定宽度 `w-8`
     - 邮箱和昵称列：使用 `flex-1` 或 `w-full` 自动填充剩余空间
     - 角色、启用状态和状态列：固定宽度
     - 操作列：固定宽度 `w-52`

3. **改进头部搜索区域**
   - 将搜索输入框容器的 `w-56` 改为 `flex-1 max-w-md`，使其在不同屏幕尺寸下自适应
   - 确保头部元素（标题、搜索框、按钮）正确对齐和分布

4. **调整卡片内边距和间距**
   - 调整 `CardContent` 的内边距，确保表格边缘与容器对齐
   - 优化组件间的间距，减少不必要的空白

5. **确保响应式设计**
   - 确保所有组件在不同屏幕尺寸下正确显示
   - 调整关键元素的尺寸和位置，以适应小屏幕设备

6. **修复虚拟滚动实现**
   - 确保虚拟滚动容器正确填充 `CardContent` 的可用空间
   - 验证虚拟滚动在不同屏幕尺寸下的表现

### 预期效果
- 界面充分利用整个视口空间，无明显空白区域
- 表格占满可用宽度，列宽分配合理
- 头部和底部元素固定，只有表格内容滚动
- 响应式设计，适应不同屏幕尺寸
- 组件对齐和间距合理，保持良好的可用性

### 实现步骤
1. 修改 `users.tsx` 中的 `parentRef` 位置和容器高度计算
2. 调整表格和列宽样式
3. 优化头部搜索区域布局
4. 调整卡片内边距和间距
5. 测试不同屏幕尺寸下的响应性
6. 验证虚拟滚动功能正常